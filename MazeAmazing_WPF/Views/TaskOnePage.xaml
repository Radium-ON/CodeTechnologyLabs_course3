<UserControl x:Class="MazeAmazing_WPF.Views.TaskOnePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:uc="clr-namespace:MazeAmazing_WPF.Views.UserControls"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel>
                <Viewbox Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Top" MaxHeight="650" MaxWidth="800">
                    <uc:MazeControl SnapsToDevicePixels="True"
                        MazeGrid="{Binding Maze}"
                        SolutionPathList="{Binding SolutionList}"
                        StartMazePosition="{Binding StartCellPosition}"
                        ExitMazePosition="{Binding ExitCellPosition}"
                        />
                </Viewbox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
